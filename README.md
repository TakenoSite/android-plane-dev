# default

Android の最小構成サンプルアプリです。アプリ起動時に画面中央へ `Hello World` を表示します。

## Requirements

- Android SDK
- JDK 17+

`sdk.dir` は `local.properties` で設定します。

## Commands

```bash
make run    # debug APK をビルド
make test   # ユニットテスト実行
make clean  # 生成物を削除
```
